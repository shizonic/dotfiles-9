KP_{Add,Subtract}
  pactl set-sink-volume 1 {+,-}2%

KP_Insert
  pactl set-sink-mute 1 toggle

super + {_,shift + }Return
  termite{_, -c .config/termite/config-gruvbox-dark}

super + d 
  PATH=$PATH:~/.local/bin rofi -show run
